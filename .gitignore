# Gradle
.gradle/
build/
.kotlin/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
# リポジトリ直下のIntelliJ既定出力先のみ対象（adapter.out.* パッケージのソースを誤って除外しないよう先頭を固定）
/out/

# Node / Next.js
node_modules/
.next/
next-env.d.ts
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Env / secrets — 秘密情報はコミットしない（Secrets Manager / SSMで管理）
.env
.env.*
!.env.example
*.pem
*.p12

# Test outputs
coverage/
test-results/
playwright-report/
*.tsbuildinfo

# ローカル動作確認用の実行ログ（gradlew bootRun / npm run dev のリダイレクト先）
/*-run.log

# Terraform
.terraform/
*.tfstate
*.tfstate.*
